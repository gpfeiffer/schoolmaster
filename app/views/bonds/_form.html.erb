<h1><%= @bond.molecule %></h1>

<%= form_for(@bond) do |f| %>
  <% if @bond.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bond.errors.count, "error") %> prohibited this bond from being saved:</h2>

      <ul>
      <% @bond.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :molecule_id %>

  <div class="field">
    <%= f.label :atom_id %><br />
    <%= f.select :atom_id, Atom.all.map { |x| [x, x.id] }, :include_blank => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
