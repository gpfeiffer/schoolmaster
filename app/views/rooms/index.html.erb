<h1>Postgraduate rooms</h1>

<table>
  <tr>
    <th>Location</th>
    <th>Capacity</th>
  </tr>

<% @rooms.each do |room| %>
  <tr>
    <td><%= link_to room.location, room %></td>
    <td><%= room.capacity %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Room', new_room_path %>
