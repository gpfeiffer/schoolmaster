<%= form_for(@supervision) do |f| %>
  <% if @supervision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supervision.errors.count, "error") %> prohibited this supervision from being saved:</h2>

      <ul>
      <% @supervision.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :student_id %>

  <div class="field">
    <%= f.label :academic_id %><br />
    <%= f.select :academic_id, options_from_collection_for_select(Academic.all, 'id', 'name') %>
  </div>

  <div class="field">
    <%= f.label :status %><br />
    <%= f.select :status, Supervision::STATUSES %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
